server {
    listen 80;
    server_name _;

    # Diretório onde os arquivos estáticos foram copiados
    root /usr/share/nginx/html;
    index index.html;

    # Arquivos estáticos com cache básico
    location /assets/ {
        try_files $uri =404;
        access_log off;
        expires 7d;
    }

    # Fallback para index.html (seguro para SPA)
    location / {
        try_files $uri /index.html;
    }
}

